import React,{useEffect,useState} from 'react'; import {api,Risk,Loading,ErrorBox} from './shared';
export default function Activities({setSelected}){const[items,setItems]=useState([]),[q,setQ]=useState(''),[status,setStatus]=useState(''),[err,setErr]=useState(''),[loading,setLoading]=useState(true); const load=()=>{setLoading(true);api(`/activities?q=${encodeURIComponent(q)}&status=${encodeURIComponent(status)}`).then(setItems).catch(e=>setErr(e.message)).finally(()=>setLoading(false))};useEffect(load,[]);return <><p className="lead">Search, filter and open every execution activity.</p><ErrorBox message={err}/><div className="filters"><input placeholder="Search activity or ID…" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()}/><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">All status</option><option>ON TRACK</option><option>AT RISK</option><option>DELAYED</option></select><button className="primary" onClick={load}>Search</button></div><div className="panel table-wrap">{loading?<Loading/>:<table><thead><tr><th>ID</th><th>Activity</th><th>Planned</th><th>Actual</th><th>Risk</th><th>Status</th></tr></thead><tbody>{items.map(a=><tr onClick={()=>setSelected(a.id)} key={a.id}><td>{a.id}</td><td><b>{a.name}</b><small>{a.project_name}</small></td><td>{a.planned_progress}%</td><td>{a.actual_progress}%</td><td><Risk value={a.risk}/></td><td>{a.status}</td></tr>)}</tbody></table>}</div></>}
